/**
 * Перечисление хранилищ данных
 *
 * @readonly
 * @enum {string}
 */
export enum Stores {
  AUDIO = 'audio',
  PLAYLIST = 'playlist',
  USER_PLAYLIST = 'user-playlist',
  USER_AUDIO = 'user-audio',
}
